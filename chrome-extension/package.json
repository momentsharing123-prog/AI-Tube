{
  "name": "aitube-chrome-extension",
  "version": "1.0.3",
  "description": "AI Tube Chrome Extension",
  "scripts": {
    "build": "tsc && node -e \"const fs = require('fs'); ['popup.js', 'options.js', 'types.js'].forEach(f => { if (fs.existsSync(f)) { const content = fs.readFileSync(f, 'utf8'); fs.writeFileSync(f, content.replace(/^export \\{\\};?\\s*$/gm, '')); } });\" && node -e \"const fs = require('fs'); if (fs.existsSync('types.js') && fs.readFileSync('types.js', 'utf8').trim() === '') fs.unlinkSync('types.js');\"",
    "watch": "tsc --watch",
    "clean": "rm -f *.js *.js.map",
    "package": "npm run build && node scripts/package.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.3"
  }
}
